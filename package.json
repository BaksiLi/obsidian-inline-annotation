{
  "name": "obsidian-inline-annotation",
  "version": "0.1.0",
  "private": true,
  "description": "Render Inline Annotation syntax in Obsidian reading view.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "check": "npm run build && node scripts/check-package.mjs"
  },
  "dependencies": {
    "markdown-it-inline-annotation": "^0.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.0.0"
  },
  "license": "MIT"
}
